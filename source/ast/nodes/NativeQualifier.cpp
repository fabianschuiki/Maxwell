/* Automatically generated by ast-gen. DO NOT MODIFY. */
#include "NativeQualifier.hpp"
#include "ast/Coder.hpp"
#include <cstdio>
#include <sstream>
#include <stdexcept>
using ast::NativeQualifier;
using ast::NodePtr;
using ast::NodeVector;

NativeQualifier::NativeQualifier() : Node(),
	interfaceGraph(this),
	interfaceNamed(this) {}

bool NativeQualifier::isKindOf(Kind k) {
	if (Node::isKindOf(k)) return true;
	return k == kNativeQualifier;
}

bool NativeQualifier::implements(Interface i) {
	if (Node::implements(i)) return true;
	if (i == kGraphInterface) return true;
	if (i == kNamedInterface) return true;
	return false;
}

NodePtr NativeQualifier::copy() {
	Ptr c (new NativeQualifier);
	Node::copy(this->graphPrev, c->graphPrev);
	Node::copy(this->name, c->name);
	return c;
}

bool NativeQualifier::equalTo(const NodePtr& o) {
	const shared_ptr<NativeQualifier>& other = boost::dynamic_pointer_cast<NativeQualifier>(o);
	if (!other) return false;
	if (!equal(this->graphPrev, other->graphPrev)) return false;
	if (!equal(this->name, other->name)) return false;
	return true;
}

std::string NativeQualifier::describe(int depth) {
	std::stringstream str, b;
	if (depth == 0) return "NativeQualifier{…}";
	str << "NativeQualifier{";
	if (this->graphPrev) b << "\n  \033[1mgraphPrev\033[0m = \033[36m" << this->graphPrev.id << "\033[0m";
	if (!this->name.empty()) b << "\n  \033[1mname\033[0m = \033[33m\"" << this->name << "\"\033[0m";
	string bs = b.str();
	if (!bs.empty()) str << bs << '\n';
	str << "}";
	return str.str();
}


void NativeQualifier::setGraphPrev(const NodePtr& v) {
	if (!v && graphPrev) {
		modify("graphPrev");
		graphPrev.reset();
	}
	if (!graphPrev || v->getId() != graphPrev.id) {
		modify("graphPrev");
		graphPrev.set(v);
	}
}

void NativeQualifier::setGraphPrev(const NodeId& v) {
	if (v != graphPrev.id) {
		modify("graphPrev");
		graphPrev.set(v);
	}
}

const NodePtr& NativeQualifier::getGraphPrev(bool required) {
	const NodePtr& v = graphPrev.get(repository);
	if (required && !v) {
		throw std::runtime_error("Node " + getId().str() + " is required to have graphPrev set to a non-null value.");
	}
	return v;
}


void NativeQualifier::setName(const std::string& v) {
	if (!equal(v, name)) {
		modify("name");
		name = v;
	}
}

const std::string& NativeQualifier::getName(bool required) {
	const std::string& v = name;
	if (required && v.empty()) {
		throw std::runtime_error("Node " + getId().str() + " is required to have a non-empty string name set.");
	}
	return v;
}


void NativeQualifier::encode(Encoder& e) {
	e.encode(this->graphPrev);
	e.encode(this->name);
}

void NativeQualifier::decode(Decoder& d) {
	d.decode(this->graphPrev);
	d.decode(this->name);
}


void NativeQualifier::updateHierarchyOfChildren() {
}

const NodePtr& NativeQualifier::resolvePath(const std::string& path) {
	size_t size = path.size();
	// graphPrev.*
	if (size >= 9 && path[0] == 'g' && path[1] == 'r' && path[2] == 'a' && path[3] == 'p' && path[4] == 'h' && path[5] == 'P' && path[6] == 'r' && path[7] == 'e' && path[8] == 'v') {
		// graphPrev
		if (size == 9) {
			return getGraphPrev();
		} else if (path[9] == '.') {
			return getGraphPrev()->resolvePath(path.substr(10));
		}
	}
	throw std::runtime_error("Node path '" + path + "' does not point to a node or array of nodes.");
}


/** Debugging some stuff. */

type A {}
type B {}

func cast (int i) -> (A a) {}
func cast (A a) -> (int i) {}
func cast (A a) -> (B b) {}
func cast (B b) -> (A a) {}

func show (int a, int b)
{
	inline { printf("a = %i, b = %b\n", (int)*@a@, (int)*@b@); }
}

func show (A a)
{
	inline { printf("A of size %i\n", (int)sizeof(*@a@)); }
}

func main
{
	A z;
	int x = 35;
	int y = 98;
	any a = x;
	A b = a;
	show(x, y);
}

#!/usr/bin/php
<?php

/**
 * mwc
 * 
 * The Maxwell compiler.
 */

require_once __DIR__.'/../source/autoload.php';
error_reporting(E_ALL ^ E_NOTICE);

global $enableColors;
$enableColors = (`tput colors` > 2);

$driver = new mwc\Driver;
$driver->configure(array_slice($argv, 1));
$driver->run();

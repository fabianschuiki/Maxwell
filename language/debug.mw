/** Debugging some stuff. */

type A {}
type B {}

func cast (int i) -> (A a) {}
func cast (A a) -> (int i) {}
func cast (A a) -> (B b) {}
func cast (B b) -> (A a) {}

func show (int a, int b)
{
	inline { printf("a = %i, b = %b", (int)*@a@, (int)*@b@); }
}

func main
{
	A z;
	int x = 35;
	any a = x;
	A b = a;
	inline { printf("a = %i\n", (int)*@a@); }
}

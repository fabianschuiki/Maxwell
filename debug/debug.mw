/** Debugging some stuff. */

type Point
{
	var int x;
	var int y;
}

func test (int a) -> (int b)
{
	var int x = a;
	var int y = b;
}

func test (float a) -> (int b)
{
}

type Application
{
	var int status;
	
	func run () -> (int a, int b)
	{
		var x = 5;
		var y = 10;
		var (int, int) p = (x, y);
	}
}

func main
{
	//var int x = 5;
	/*var int y = x;
	var (int, int) p = (1, 2);
	var t = (a: 25, b: 64.97e47);
	var (float, float) point = t;
	var z = x;
	var tup = (x, z, 5);
	var (int, int, float) tupfixed = tup;*/
	//var int res_a = test(x);
	//var float res_b = test(x);
	
	/* This doesn't work yet, as the compiler doesn't choose between float and int. This will come with assumptions later. */
	//var int res_c = test(123);
	var Application app;
	var x = run(app);
	var a;
	var b;
	var p1 = (a, b);
}

/** Debugging some stuff. */

type object {}
type scalar {}
type primit primitive {}

func (+) (object a, object b) -> (object r) {}
func (+) (scalar a, scalar b) -> (scalar r) {}
func (+) (primit a, primit b) -> (primit r) {}

func main
{
	object ox;
	object oy;
	object oz = ox + oy;
	
	scalar sx;
	scalar sy;
	scalar sz = sx + sy;
	
	primit px;
	primit py;
	primit pz = px + py;
	
	int ix = 1;
	int iy = 2;
	int iz = ix + iy;
}

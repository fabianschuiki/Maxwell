/* Copyright © 2013 Fabian Schuiki */
package main
import "util/String"
import "util/Console"

func main
{
	// Define a counting target.
	var int target = 10;
	"Up to what number would you like to count? ".println();
	target = read();
	"I'm going to count up to %!".format(target).println();

	// Actually do the counting.
	for (var int i = 0; i < target; i = i + 1) {
		"- now at %".format(i).println();
	}
}
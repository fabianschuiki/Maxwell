/** Debugging some stuff. */

// should resolve to (a: int, b: int) -> ()
func spec (a, b)
{
	var (int, int) tuple = (a, b);
}

// should resolve to (a: *, b: *) -> ()
func gen (a, b)
{
	var tuple = (a, b);
}

func main
{
	var int x;
	var int y;
	gen(x, y);
	
	var float a;
	var float b;
	gen(a, b);
	gen(a, b);
}

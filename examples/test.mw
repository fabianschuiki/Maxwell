/** Debugging file for Maxwell language. */

//Function without arguments.
testMethod {}

//Function with only input arguments.
testInput (double a, double r) => (double res) {}

//Function with only output arguments.
testInput => double {}


//Simple function.
test (double a, double b) => double
{
	//Calculate a simple sum.
	return a + 5;
}

/** Debugging some stuff. */

type Point
{
	int x;
	int y;
}

func main
{
	Point p;
	inline { @p@ = malloc(sizeof(Point_t)); }
	p.x = 10;
	p.y = 20;
	
	//Dump the point.
	inline { printf("point: %i x %i\n", (int)@p@->x, (int)@p@->y); }
}

type Foo interface {
	func hello (@, any);
}

type Bar interface {
	func hello (any, @);
}

type A object {};
type B object {};

func hello (A, int);
func hello (int, B);
func hello (A, B);

// A is now considered Foo
// B is now considered Bar

func world (Foo f, Bar b)
{
	hello(f,b);
}
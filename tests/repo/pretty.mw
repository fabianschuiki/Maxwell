// 1.1
func debug (real x) -> (int y)
{
    var z = x;
    // type of expr = real
    // type of expr.initialExpr = real
    // x -> 1.1.inputs.arguments.0
    y = z;
    // type of expr = int
    // type of expr.lhs = int
    // y -> 1.1.outputs.arguments.0
    // type of expr.rhs = int(real,1)
    // z -> 1.1.body.stmts.0.expr
}
/* Automatically generated by ast-gen. DO NOT MODIFY. */
#pragma once
#include "../Node.hpp"
#include "interfaces.hpp"
#include "types.hpp"

namespace ast {

class Encoder;
class Decoder;

class CastType : public Node
{
public:
	// constructor
	CastType();

	// auxiliary functions
	virtual bool isKindOf(Kind k);
	virtual bool implements(Interface i);
	virtual std::string getClassName() const { return "CastType"; }
	virtual NodePtr copy();
	virtual bool equalTo(const NodePtr& o);
	virtual std::string describe(int depth = -1);

	// accessor functions
	void setFunc(const NodePtr& v);
	void setFunc(const NodeId& v);
	const NodePtr& getFunc(bool required = true);

	void setIn(const NodePtr& v);
	const NodePtr& getIn(bool required = true);

	void setOut(const NodePtr& v);
	const NodePtr& getOut(bool required = true);

	// encoding and decoding
	virtual void encode(Encoder& e);
	virtual void decode(Decoder& d);

	// hierarchy functions
	virtual void updateHierarchyOfChildren();
	virtual const NodePtr& resolvePath(const std::string& path);
	virtual NodeVector getChildren();

	// shared_ptr convenience
	typedef boost::shared_ptr<CastType> Ptr;
	template<typename T> static Ptr from(const T& n) { return boost::dynamic_pointer_cast<CastType>(n); }
	template<typename T> static Ptr needFrom(const T& n) {
		Ptr r = boost::dynamic_pointer_cast<CastType>(n);
		if (!r)
			throw std::runtime_error("Node " + n->getId().str() + " (a " + n->getClassName() + ") cannot be dynamically cast to CastType.");
		return r;
	}

protected:
	NodeRef func;
	NodePtr in;
	NodePtr out;
};

} // namespace ast
